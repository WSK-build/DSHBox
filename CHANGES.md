# CHANGES — v1.1.0 变更记录（相对 v1.0.0）

> 本文件只记录 v1.1.0（versionCode 3）相对 v1.0.0 的变更；完整历史见发布包根文档 `CHANGELOG.md`。

## 修复

- **离线导入运行环境包必失败**（官方 zip 的 `<layer>.tar.zst.sha256` 侧车文件满足旧的 `startsWith` 匹配、覆盖同名层归档，最终报 `Not in GZIP format`）。修复：层归档精确匹配、Zip-Slip 路径穿越防护、兼容带一层目录前缀的 zip、强制 `runtime-profile.json` 存在、侧车与 profile 声明交叉核对。
- **DSH 离线导入版本变 unknown、重启后被内置层静默覆盖**（层包 stub `package.json` 的 UTF-8 BOM 使版本解析失败；内置版本推导口径不一致）。修复：BOM 容错、优先读取真实产品版本、内置版本推导归一化。
- **损坏/截断/加密 zip、复制中断导致闪退**。修复：导入全链路异常转为可读失败提示（按实测异常形态映射文案），扫描/清理标志位不再死锁。

## 新增 / 重构

- **更新 DSH（在线）新界面**：并行探测各 npm 源（官方 / 阿里 npmmirror / 腾讯云 / 华为云）的版本号与网络延迟；选源选版本（降级二次确认）；在沙箱内用 npm 从所选源拉取 `@deepseek-ai/dsh` 及完整依赖树（复刻官方构建方式 `install_dsh.sh`）；自动重启生效。实时日志流、可取消、退出界面不中断安装。
- **存储占用与清理**：占用按系统同口径统计（分配块、含应用缓存、进设置页自动刷新、可手动刷新）；新增「清理缓存与垃圾文件」（应用缓存 / 访客临时文件 / 运行日志 / apt 缓存，逐项勾选；回滚备份可选）。清理与后台安装/导入互斥，绝不触碰 `user-data/.dsh` 与运行环境本体。
- **导入格式全覆盖**：层归档支持 `.tar.zst / .tar.gz / .tar / .tgz / .bz2 / .xz`（按魔数识别）；支持裸 `base.tar` 层归档与「外层 tar 装层归档」布局；DSH 层安装改为原子换层（暂存区形态校验，失败不留半成品）。
- **版本比较语义修正**：正式版本不再被同号预发布版本压制（semver 惯例）。

## 测试

197 个 JVM 单测（common / sandbox-manager / app / terminal-*）全部通过；`assembleDebug` / `assembleRelease` 构建通过。
