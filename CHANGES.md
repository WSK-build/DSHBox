# CHANGES — 版本变更记录

按版本号倒序记录每个版本的变更要点。

| 版本 | versionCode | 日期 | 主题 |
|---|---|---|---|
| v1.3.1 | 7 | 2026-09-11 | DSH 0.1.5-rc.2 深度适配：WebView 多版本兼容 + 内置组件打通 + 运行时垫片架构 |
| v1.3.0 | 6 | 2026-09-09 | 多语言版本（联合国六语界面 + 语言选择器）+ App 更新检测 + 官网 |
| v1.2.0 | 5 | 2026-09-08 | 文件管理增强：移动到指定文件夹 + 通用查看器/编辑器 |
| v1.1.1 | 4 | 2026-09-04 | 在线更新全链路修复 + DSH 0.1.2-rc.1 兼容 |
| v1.1.0 | 3 | 2026-08-31 | 离线导入修复 + 在线更新重构 + 存储占用与清理 |
| v1.0.0 | 2 | 2026-08-25 | 正式开源（Apache-2.0 → GPL v3）：运行环境分层架构 |
| v0.1.0 | 1 | 2026-08-17 | 开源预览版：PRoot 沙箱运行 DSH |

---

## v1.3.1（versionCode 7）— 2026-09-11

DSH 0.1.5-rc.2 深度适配版本。

本版主线是**让最新的 DSH 在安卓 WebView 里真正跑通**：内置 DSH 由 `0.1.1-rc.2` 升级到 `0.1.5-rc.2`，随之而来的会话持久化、文件工具、附件发布、右栏预览、设置面板等组件在移动端的断点被逐个打通；同时把「硬链接兼容」从改写 DSH 源码改为运行期垫片，并为不同 WebView 内核版本做了兼容适配。

**架构**

- **硬链接兼容改为运行期垫片**：不再在安装期改写 DSH 的 JS 文件（旧方案要求补丁锚点与上游源码逐字节匹配，上游一调整就整块静默失效），改为启动时以 `node --import` 预加载垫片、在运行期替换 `node:fs/promises` 的 `link`——平台拒绝硬链接时退化为语义等价的内容拷贝。**DSH 源码一个字节都不改**，从此不受上游重构影响。
- 内置 DSH 层升级至 **0.1.5-rc.2**（纯净上游，不打补丁）。

**修复**

- DSH 0.1.5 下会话持久化、写入工具、附件发布三条链路在真机上全部 EACCES（Android 应用数据分区 FBE/FUSE 拒绝硬链接）。
- 网页端上传附件无任何反应：WebView 缺 `onShowFileChooser` 实现。
- 右栏连同文件预览面板打不开（适配样式误用 `display:none` 隐藏了整列）。
- **「文件资源服务不可用」**：右栏点文件报错。根因是不同 WebView 版本对非标准 scheme 的 URL 解析差异——部分内核不解析 authority，取到的 host 为空。已在插件内做**运行期能力探测 + 范围锁定**的适配，新内核零改动。
- 「打开配置文件」在全新设备上必然失败（拦截后上游的建文件步骤不再执行）。
- 上传来源菜单曾挂错按钮：接管了「+」命令面板，已改挂回形针并把「+」归还 DSH。
- 沙箱上传后隐藏 file input 的 `accept` 哨兵值残留，会污染后续的 DSH 原生上传入口。

**新增**

- **移动端适配包装配自动化**：已装配的用户在 APK 升级后，启动时自动把 profile 内的插件副本刷新为当前版本（含「已是最新」判定，不覆盖用户的手改）。
- 诊断页新增 **WebView 真实内核指纹**：提供者 / 版本 / Chromium 主版本 + `:has()` / `color-mix()` / `subgrid` / `light-dark()` / `dvh` 等 CSS 能力的**实机探测**——内核过旧时这些特性会静默失效（不报错、无异常），只能实测。
- 设置新增「**用户反馈**」分区：GitHub Star 与问题反馈两条入口，均先弹窗说明再跳转。
- 上传来源菜单（回形针）：沙箱文件 / 从安卓导入等来源选择。

**体验**

- 「诊断」更名为「诊断与日志」，并与其他设置项统一为圆角卡片 + 分隔线的规范布局。
- 官网移除整页滚动吸附（手机上「一滑跨好几屏」且拖动会被弹回），截图区改双列。

**验证**：JVM 单测 **414 例**通过；i18n 六语一致性门禁通过；`assembleRelease` 含 lintVital 通过；真机（vivo V2282A / Android 15）逐项验证——会话与附件写入、上传、右栏预览、配置文件打开、跨 WebView 版本适配均闭环。

---

## v1.3.0（versionCode 6）— 2026-09-09

多语言版本。

**新增**

- 多语言适配：界面支持联合国六种官方语言——English / 简体中文 / العربية / Español / Français / Русский，默认跟随系统、未支持语言回退英文
- 设置 → 外观新增「语言」选择器：六语 + 跟随系统，即选即生效；通知、弹窗、终端菜单等均随语言切换；数量文案按语言复数规则处理（阿拉伯语 6 形态 / 俄语 4 形态）
- 深色主题贯穿文件管理在内的全部页面（此前文件管理区恒浅色）
- App 更新检测：设置「检查更新」联网比对 GitHub Releases 最新版本，发现新版弹窗引导至官网下载，并支持「忽略该版本」；启动时静默检测一次
- GitHub Pages 官网（`website/`）随仓库发布，作为下载与介绍入口

**优化**

- 布局方向：全局恒 LTR（阿拉伯语文本按 Unicode bidi 正常渲染），避免整页镜像
- 内置 i18n 一致性门禁（六语键位 / 占位符 / 复数形态 / 硬编码扫描）并接入 CI

**验证**：JVM 单测 214 例通过；`assembleRelease` 含 lintVital 通过；六语资源均打入 APK；真机装机已验证。

---

## v1.2.0（versionCode 5）— 2026-09-08

文件管理增强。

**新增**

- 移动到指定文件夹：行菜单 / 多选「移动到…」→ 全屏目标选择器（沙盒 / 工作区切换、面包屑、可新建文件夹、源目录树置灰防环）；冲突三策略（覆盖 / 跳过 / 自动改名，可应用到其余全部）；同卷 `renameTo` 优先、失败复制兜底；跨层移动强确认。「重命名」统一走同一移动引擎。
- 通用文件查看器 / 编辑器（任何文件必有界面）：文本 / 代码编辑（Sora Editor、语法高亮、编码探测与切换、大文件分级、原子保存）、图片（缩放 / GIF / 长图 / AVIF）、PDF（分页、加密密码）、压缩包浏览（zip 与 tar 系、包内预览、条目导出）、十六进制（三栏 + 熵估计）、docx / xlsx 抽文本、Markdown / HTML / SVG 预览、外部应用打开 / 编辑 / 分享。
- 导入增强：「从安卓导入」支持多选批量与压缩包解压导入，逐件冲突决策；ZIP 中文名编码修复。

**优化**

- 统一物理层判定（`layerOf`），风险保护覆盖嵌套 `.dsh` 与系统目录。
- 新增依赖 sora-editor、markwon 等；零新增存储权限、零新增 native .so。

**验证**：JVM 单测 172/172 通过；真机（vivo V2282A / Android 15）全部清单验证闭环。

---

## v1.1.1（versionCode 4）— 2026-09-04

在线更新全链路修复。

**修复**

- 点「安装」整个 App 闪退：安装进度视图嵌套滚动测量崩溃。
- 换层前旧 DSH 进程从未被停掉（停机条件恒假），换层后 EADDRINUSE 起不来 → 停机 / 重启路径统一加 cmdline 树杀兜底。
- 「取消安装」无效：SIGTERM 对 PRoot 无效 → 按 `/proc` 杀整棵安装进程树，约 1 秒收敛。
- npm 下载缓存写入运行环境红线区清不掉（实测 446MB）→ 迁至应用缓存目录，可一键清理。

**兼容**

- DSH 0.1.2-rc.1：健康检查接受 401 响应（不再误判未就绪）；WebView 从启动输出解析 launchToken，自动完成网页会话认证。

**增强**

- 首次安装显示已用时 / 预计耗时，全程后台运行，可离开页面。
- 装配移动端适配包支持一键移除、反复切换。
- 进程日志 2MB 自动轮转（保留最近两代）；诊断页升级为 DSH / 沙箱 / 访客命令三日志条目，可滚动、可导出。
- guest 命令等待加 10 分钟超时兜底。

---

## v1.1.0（versionCode 3）— 2026-08-31

离线导入修复 + 在线更新重构 + 存储管理。

**修复**

- 离线导入运行环境包必失败：zip 内 `.sha256` 侧车误匹配覆盖层归档 → 精确匹配 + Zip-Slip 防护 + 校验交叉核对。
- DSH 离线导入版本变 unknown、重启被内置层覆盖（package.json BOM + 版本仲裁缺陷）。
- 损坏 / 截断 / 加密 zip 直接闪退 → 全链路异常转为可读提示。

**新增**

- 更新 DSH（在线）新界面：并行探测各 npm 镜像（官方 / 阿里 / 腾讯云 / 华为云）版本与延迟 → 选源选版本（降级二次确认）→ 沙箱内 npm 拉取完整依赖树 → 自动重启；实时日志、可取消、后台安装。
- 存储占用按系统同口径统计（分配块、含应用缓存）；「清理缓存与垃圾文件」按项勾选（应用缓存 / 访客临时文件 / 运行日志 / apt 缓存，回滚备份可选），与后台安装互斥，不触碰 `user-data/.dsh`。

**增强**

- 层归档格式全覆盖：`.tar.zst / .tar.gz / .tar / .tgz / .bz2 / .xz`（按魔数识别），支持裸 `base.tar` 与外层 tar 布局。
- 版本比较器修正「正式版 < 预发布版」的语义缺陷。

---

## v1.0.0（versionCode 2）— 2026-08-25

正式开源：自本版起许可证由 Apache-2.0 改为 **GPL v3**。

- 运行环境由单体 bundle 重构为分层：base（Debian）+ node + android-side，zstd 压缩，逐层 SHA-256 哨兵与完整性校验，运行时按层 bind 组装。
- DSH 独立层：版本仲裁（已装较新则保留）、`previous/dsh` 单份回滚、Android 硬链接兼容补丁；APK 内置 DSH 层包，首启自动装配。
- 设置页新增：更新 DSH（在线 / 离线导入）、离线导入运行环境包、装配 DSH 移动端适配包（cordis 插件 `@local/dsh-mobile-adapt`）。
- 终端工具包内置（vim / htop 等 deb 免下载）；修复 htop 因 SELinux 读不到 `/proc/stat` 的崩溃。

---

## v0.1.0（versionCode 1）— 2026-08-17

**开源预览版本：PRoot 沙箱运行 DeepSeek Harness**

Android 原生 UI + PRoot 用户态 Linux 沙箱运行 DSH；运行环境内嵌于 APK（约 345MB，首次启动自动解包，需 1~3 分钟）。DSH WebUI 当时通过系统浏览器使用（`http://127.0.0.1:3080`）——内嵌 WebView、持久终端、底部五标签导航均为后续版本引入。

当时已具备的功能：

- DSH Agent（WebUI 内）：模型对话、文件工具、bash 终端、子代理、目标管理
- 文件页：沙盒文件 / 工作区双视图浏览、导入 / 导出引导、目录 ZIP、搜索、排序、网格 / 列表切换
- 沙盒终端：App 内持久 shell 会话
- 运行环境管理：内置运行环境、更新导入、回滚
- 前台服务保活

浏览器适配提示：安卓平板浏览器效果最佳；手机浏览器页面较拥挤（可横屏）；建议 Chrome「添加到主屏幕」获得更稳定显示。

该版本以 Apache-2.0 发布（commit：`Initial release: DSHBox v0.1.0 - DeepSeek Harness Mobile Agent Sandbox`）；自 v1.0.0 起改为 GPL v3 并正式开源。
